
## short bash script

arg $1 is set to the variable `MORSE_CODE` if it exists. Otherwise is set
to `"- . ... - .. -. --.|... - .-. .. -. --."` which will translate to
`TESTING STRING`.

## usage

`chmod +x runtest.sh`

`./runtest`
or
`./runtest "-.-. ... ...-- ..--- .---- -----|.--. .----"`

